package com.scala.traits

/**
 * Basic Node trait for classes to implement
 * in order to build an Abstract Syntax Tree
 * with nodes that all come from the same base
 * type
 */
trait tNode {
	var token: tToken
}
